[package]
name = "shift-schedule-generator"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = "0.4.40"
clap = { version = "4.5.36", features = ["derive"] }
inquire = { version = "0.7.5", features = ["date"] }
